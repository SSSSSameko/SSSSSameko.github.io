# 微博发布文案

微博转发抽奖助手 3.0.0 更新完成。

这次主要优化了 UI、候选载入、错误提示、开奖结果和开奖记录，并修复 JSON 名单导入时昵称被拆开的问题。更多页面新增意见反馈，提交的内容可以直接在管理后台查看。

服务器侧同步加强了接口限流、请求体限制、后台登录、Cookie 写入保护和运行状态诊断。Cookie 保活仍为默认每 12 小时一次，失败后按计划重试。

完整版本记录可在“更多 → 关于此应用 → 更新日志”中查看。
