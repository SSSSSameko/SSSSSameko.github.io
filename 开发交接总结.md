# Sameko 微博抽奖网站开发交接总结

整理日期：2026-09-12

## 当前结论

唯一保留的代码基线是 `D:\开发\sameko-weibo-lottery`。它已经包含完整的
Git 历史、标签、远端配置、测试和部署资料，不再依赖
`C:\Users\35110\Documents\微博抽奖网页` 或
`C:\Users\35110\Desktop\GitHub\11z\weibo-redpacket-probe` 中的工作树。

| 项目 | 内容 |
| --- | --- |
| 当前版本 | `3.4.0` |
| 上游发布基线 | `fab456434473b3655f250386a8018ffd052dcfae` |
| Git 标签 | `v3.4.0` |
| 默认分支 | `main` |
| 远端仓库 | `https://github.com/SSSSSameko/SSSSSameko.github.io.git` |
| 前端 | GitHub Pages 静态站点 |
| 后端 | `https://111.228.11.206`，Node.js + systemd + Caddy |

## 产品与功能

- 微博公开转发候选抓取，支持主入口、备用入口、分页去重、节流和重试。
- 手动名单、JSON 导入、搜索、筛选、导出和大名单操作。
- 多奖项、候补、随机种子、滚动开奖、开奖确认和防重复中奖。
- 开奖回执、结果图、CSV、复制公示文案、历史备份恢复和服务器记录管理。
- 服务器 Cookie 池、扫码登录、自动保活、状态诊断和后台管理。
- 深色主题、桌面宽屏布局、键盘导航、无障碍和减少动态效果偏好。
- 独立只读登录态接口 `GET /v1/cookie/current`，供其他可信服务调用。

## 版本时间线

- `3.0.0`（2026-08-23）：重构主要流程，统一抽奖、名单、记录、更多页面。
- `3.1.0`（2026-08-25）：优化开奖体验，增加开奖前确认。
- `3.2.0`（2026-08-31）：增加名单搜索、详情、导出，完善记录恢复和长任务稳定性。
- `3.3.0`（2026-09-04）：加入深色主题、无障碍、记录存储加固和法律许可修正。
- `3.3.1` / `3.3.2`（2026-09-07）：桌面宽屏工作区、显示修复、服务端优化和记录兼容。
- `3.4.0`（2026-09-10）：首屏性能、按需加载、深色模式、桌面布局和交互流畅度优化。
- `fab4564`（2026-09-11）：法律文本规范化，并修复 9 个弹窗主按钮阴影裁切。

## Codex 对话整理

原开发对话从 2026-08-27 持续到 2026-09-10。共识别 78 条属于本项目的
Codex 记录：

- 1 条用户主会话：`01a066b7-7613-7732-bd01-345ee9335373`
- 64 条子智能体会话
- 13 条用户界面中显示为“未命名会话”的 `agent_forked_thread`
- 其中 5 条是 2026-08-27 至 08-31 的早期项目子会话

开发过程的主线如下：

1. 先对旧原型做前端、后端、数据兼容、安全、视觉和发布流程的多轮只读审计。
2. 在 `release-3.0.0` 工作树中统一架构与交互，补充候选抓取、记录恢复、反馈和后台能力。
3. 对鉴权、CORS、限流、请求体积、头像 SSRF、原子写入、记录去重和进程回收逐项加固。
4. 完成深色模式、桌面宽屏、键盘导航、焦点管理、减少动画和多项显示修复。
5. 对 3.4.0 做性能与体验收尾，并补充第三方许可、隐私、使用规则和免责声明。
6. 推送 GitHub Pages，部署到现有后端服务器，验证健康检查和只读 Cookie 接口。
7. 最后一轮修复弹窗固定页脚和阴影裁切；201 项单元测试及 13 项浏览器界面回归通过。

已放弃的 `weibo-lottery-v2` 是另一套本地确定性抽取重构，不是上线版本，
也没有继续维护。它的审计结论中值得保留的原则是：抽取过程要可复验、存储失败要
关闭写入、接口边界要明确。但当前生产版本仍以微博抓取、服务器 Cookie 池和现有
开奖记录兼容为真实需求，因此不合并 V2 架构。

原始会话记录已经永久删除，本文件保留开发结论。当前整理任务会话不在此次删除范围内。

## 已迁移的本地数据

旧工作树中的本地运行数据已复制到：

`D:\开发\sameko-weibo-lottery\output\legacy-local-20260910`

包含：

- `draws\*.json`：8 条开奖记录
- `admin-events.json`
- `draw-attempts.jsonl`
- `draw-retention-policy.json`
- `system-metrics.json`

复制后已逐文件通过 SHA-256 校验。`output/` 已被 Git 忽略，这些数据只属于本机，
不要提交。旧登录 Profile、`weibo-login-state.json` 和 Cookie 没有迁移，如本地需要
抓取，重新扫码登录即可。

## 副本排查

对 `C:\Users\35110\Desktop`、`C:\Users\35110\Documents`、
`C:\Users\35110\Downloads` 和 `D:\` 做了文件名与项目标识扫描，没有发现第二份
`sameko-weibo-lottery` 源码或静态前端副本。

`C:\Users\35110\Desktop\sameko\codex\weibo-redpacket-desktop-bridge` 是独立的
版本 `1.15.2` 本机 MuMu/ADB 控制台，不包含本抽奖网站的页面或后端，也不是旧版
镜像。它属于另一个项目，本次没有修改或回收。微博爬虫研究目录同样保持不动。

## 只读 Cookie 接口

完整客户端契约见 `docs/COOKIE-API-CLIENT.md`。

- 地址：`GET https://111.228.11.206/v1/cookie/current`
- 认证头：`x-cookie-read-key` 或 `Authorization: Bearer`
- 成功：`200`，返回当前登录态及 `savedAt`、`lastValidAt`、`quarantined`
- 没有可用登录态：`503`
- 密钥错误：`401`
- 来源或 HTTPS 条件不满足：`403`
- 接口关闭：`404`

调用密钥不保存在仓库和本文档中。不要把该接口放在浏览器、小程序前端或公开日志中；
接口响应等同于微博登录态。

## 验证状态

以下检查已经通过：

- `npm ci`
- `npm test`：201/201
- `npm run build`
- `npm run licenses:check`
- `npm run legal:check`
- `npm run test:release`：完整通过，包括 9 项微博多入口回退、浏览器生命周期、
  内存浸泡和 13 项浏览器界面回归。

本机 Mihomo 曾把 `sinaimg.cn` 解析为 `198.18.0.49`，导致头像错误状态用例被
SSRF 防护提前拒绝。整理时只在 `SERVER_TEST_MODE=1` 下加入测试 DNS 注入；
生产环境仍使用真实 DNS，并继续拒绝 `198.18.0.0/15` 等私网地址。

## 最高优先级遗留风险

1. 当前法律文本写的是“运营者与后端服务器均位于美国，适用怀俄明州法律”，但开发
   会话确认 `111.228.11.206` 实际仍在天津。迁移服务器前，公开传播页面前必须修正
   文案或完成真实迁移；二者不能继续不一致。
2. 仓库内偶像图片、头像、摄影和其他视觉素材没有完整来源与授权证明。公开使用前
   必须逐项确认展示、复制、缓存、裁切和结果图生成权限，见 `docs/ASSET-NOTICES.md`。
3. `API_KEY` 会下发到浏览器，不能作为保密身份凭据。真正依赖的是来源白名单、限流、
   Caddy、云防火墙和服务器监控。
4. 服务器迁移到境外后，微博很可能把扫码登录和新 IP 抓取判为异常。迁移前应使用
   测试账号验证 Cookie 保活和抓取，再切换生产。

## 本地维护提交

本次整理新增项目规则、交接总结、本地数据迁移说明和测试 DNS 注入修复。若
`git log -1` 的提交不是 `fab4564`，该提交是本地整理提交，尚未自动推送；
`origin/main` 和 `v3.4.0` 仍以上游发布基线为准。需要同步远端时先复核差异并运行
完整发布测试。

## 新环境运行

```powershell
cd D:\开发\sameko-weibo-lottery
npm ci
npm test
npm run build
npm start
```

默认本地地址为 `http://127.0.0.1:4173/`。生产部署前阅读 `README.md`、
`docs/SECURITY-REVIEW.md` 和 `docs/COOKIE-API-CLIENT.md`。

## 清理与恢复

旧代码目录已通过 Windows 回收站方式回收：

- `C:\Users\35110\Documents\微博抽奖网页`
- `C:\Users\35110\Desktop\GitHub\11z\weibo-redpacket-probe`

需要短期恢复时，可从 Windows 回收站还原。Codex 项目会话是永久删除，不能从回收站
恢复，因此后续开发以本交接文档、Git 历史和仓库内文档为准。
