# Sameko 微博抽奖网站开发交接

整理日期：2026-09-13

本文件只保留需要跨任务延续的背景和风险。运行方法、环境变量、Cookie
接口契约和版本历史分别以 `README.md`、`docs/COOKIE-API-CLIENT.md` 和
`docs/CHANGELOG.md` 为准，不在本文重复。

## 代码基线

- 唯一维护目录：`D:\开发\sameko-weibo-lottery`
- 版本：`3.5.0`
- 默认分支：`main`
- 上游发布基线：`fab456434473b3655f250386a8018ffd052dcfae`
- 远端：`https://github.com/SSSSSameko/SSSSSameko.github.io.git`
- 前端：GitHub Pages
- 后端：`https://111.228.11.206`，Node.js + systemd + Caddy

本地整理提交可能领先于 `origin/main`。发布前必须检查
`git status --short --branch` 和 `git log --oneline --decorate -5`，确认待发布
提交，并运行 `npm run test:release`。

## 本机数据

旧工作树中的开奖数据已迁移到：

`D:\开发\sameko-weibo-lottery\output\legacy-local-20260910`

其中包含 8 条开奖记录、后台事件、开奖尝试日志、保留策略和系统指标。迁移文件
已经过 SHA-256 校验；`output/` 已被 Git 忽略，不要删除或提交。

旧登录 Profile、`weibo-login-state.json` 和 Cookie 没有迁移。本机需要抓取时，
重新扫码登录即可。

旧源码目录已通过 Windows 回收站清理，不再作为维护基线。日常开发和部署只使用
本文开头的唯一维护目录。

## 架构决策

已放弃的 `weibo-lottery-v2` 是另一套本地确定性抽取重构，不是上线版本。当前
生产版本仍以微博抓取、服务器 Cookie 池和既有开奖数据兼容为真实需求，不应直接
合并 V2 架构。

V2 审计中仍值得保留的原则是：抽取过程可复验、存储失败时关闭写入、接口边界明确。
未来若要重新设计，应作为独立方案完成兼容和迁移验证后再接入。

## Cookie 接口

只读接口为 `GET /v1/cookie/current`，完整契约见
`docs/COOKIE-API-CLIENT.md`。

- 响应等同于微博登录态，只能由可信服务端调用。
- 不要把 Cookie、调用密钥或响应写入前端、仓库、截图或日志。
- 建议轮询间隔不低于 5 分钟，发现泄漏后立即重新登录并轮换密钥。

## 上线前必须处理

1. 仓库视觉素材缺少完整来源和授权证明。公开上线或再分发前，必须按
   `docs/ASSET-NOTICES.md` 逐项确认展示、复制、缓存和结果图生成权限。
2. 安装脚本不再默认生成 `API_KEY`：公开静态前端拿不到也藏不住它，写死一个只会
   让整站 401。默认改为 `ALLOW_PUBLIC_API=1`，实际边界依赖浏览器级限流、任务
   队列、来源白名单、Caddy、云防火墙和服务器监控。确实需要访问门槛时再显式配置
   `API_KEY`，并把同一个值同步给所有前端分发渠道。


## 验证与已知环境问题

截至 2026-09-12，`npm run test:release` 与 `npm run legal:check` 均已通过。
前者包括 209 项单元测试、构建、许可检查、微博多入口回退、浏览器生命周期、内存
浸泡和 14 项浏览器界面回归。

本机 Mihomo 可能把 `sinaimg.cn` 解析为 `198.18.0.49`。测试模式只允许注入测试
DNS；生产环境继续使用真实 DNS，并拒绝 `198.18.0.0/15` 等私网地址，不得为了
通过测试而放宽 SSRF 校验。
